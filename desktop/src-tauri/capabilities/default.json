{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled start screen may call start_backend. The dashboard itself (served by the KillLine backend on 127.0.0.1) gets no access to Tauri APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
